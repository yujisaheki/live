<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>配信画面</title>
    <link rel="stylesheet" href="/css/app.css"> <!-- Add your CSS file here -->
</head>
<body>
    <h1>配信画面</h1>
    <div class="stream-box">
            <%= image_tag 'user-profile2.png', alt: '配信画像', style: 'width: 50%; height: 50%;' %>
    </div>

    <!-- Alert Messages -->
    <% if notice %>
        <div class="alert alert-success" role="alert">
            <%= notice %>
        </div>
    <% end %>
    <% if alert %>
        <div class="alert alert-danger" role="alert">
            <%= alert %>
        </div>
    <% end %>

    <div class="content-box bg-white text-dark">
        <%= yield %>
    </div>

    <!-- JavaScript -->
    <script src="<%= mix('/js/app.js') %>"></script>
</body>
</html>