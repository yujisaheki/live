<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>配信一覧</title>
    <link rel="stylesheet" href="/css/app.css"> <!-- Add your CSS file here -->
</head>
<body>
    <h1>配信一覧</h1>
    <div class="flex-box">
        <div class="thumnail-left-box">
            <% 24.times do %>
                <%= image_tag 'user-profile.png', alt: 'プロフィール画像', url: '画像のURL' %>
            <% end %>
        </div>
    </div>

    <!-- Alert Messages -->
    <% if notice %>
        <div class="alert alert-success" role="alert">
            <%= notice %>
        </div>
    <% end %>
    <% if alert %>
        <div class="alert alert-danger" role="alert">
            <%= alert %>
        </div>
    <% end %>

    <div class="content-box bg-white text-dark">
        <%= yield %>
    </div>

    <!-- JavaScript -->
    <script src="<%= mix('/js/app.js') %>"></script>
</body>
</html>